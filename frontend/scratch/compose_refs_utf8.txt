Γöî shadcn add @diceui/time-picker (dry run)
Γöé
Γö£ lib\compose-refs.ts (create) 63 lines
Γöé ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
Γöé Γöé import * as React from "react";
Γöé Γöé 
Γöé Γöé type PossibleRef<T> = React.Ref<T> | undefined;
Γöé Γöé 
Γöé Γöé /**
Γöé Γöé  * Set a given ref to a given value
Γöé Γöé  * This utility takes care of different types of refs: callback refs and RefObject(s)
Γöé Γöé  */
Γöé Γöé function setRef<T>(ref: PossibleRef<T>, value: T) {
Γöé Γöé   if (typeof ref === "function") {
Γöé Γöé     return ref(value);
Γöé Γöé   }
Γöé Γöé 
Γöé Γöé   if (ref !== null && ref !== undefined) {
Γöé Γöé     ref.current = value;
Γöé Γöé   }
Γöé Γöé }
Γöé Γöé 
Γöé Γöé /**
Γöé Γöé  * A utility to compose multiple refs together
Γöé Γöé  * Accepts callback refs and RefObject(s)
Γöé Γöé  */
Γöé Γöé function composeRefs<T>(...refs: PossibleRef<T>[]): React.RefCallback<T> {
Γöé Γöé   return (node) => {
Γöé Γöé     let hasCleanup = false;
Γöé Γöé     const cleanups = refs.map((ref) => {
Γöé Γöé       const cleanup = setRef(ref, node);
Γöé Γöé       if (!hasCleanup && typeof cleanup === "function") {
Γöé Γöé         hasCleanup = true;
Γöé Γöé       }
Γöé Γöé       return cleanup;
Γöé Γöé     });
Γöé Γöé 
Γöé Γöé     // React <19 will log an error to the console if a callback ref returns a
Γöé Γöé     // value. We don't use ref cleanups internally so this will only happen if a
Γöé Γöé     // user's ref callback returns a value, which we only expect if they are
Γöé Γöé     // using the cleanup functionality added in React 19.
Γöé Γöé     if (hasCleanup) {
Γöé Γöé       return () => {
Γöé Γöé         for (let i = 0; i < cleanups.length; i++) {
Γöé Γöé           const cleanup = cleanups[i];
Γöé Γöé           if (typeof cleanup === "function") {
Γöé Γöé             cleanup();
Γöé Γöé           } else {
Γöé Γöé             setRef(refs[i], null);
Γöé Γöé           }
Γöé Γöé         }
Γöé Γöé       };
Γöé Γöé     }
Γöé Γöé   };
Γöé Γöé }
Γöé Γöé 
Γöé Γöé /**
Γöé Γöé  * A custom hook that composes multiple refs
Γöé Γöé  * Accepts callback refs and RefObject(s)
Γöé Γöé  */
Γöé Γöé function useComposedRefs<T>(...refs: PossibleRef<T>[]): React.RefCallback<T> {
Γöé Γöé   // biome-ignore lint/correctness/useExhaustiveDependencies: we want to memoize by all values
Γöé Γöé   return React.useCallback(composeRefs(...refs), refs);
Γöé Γöé }
Γöé Γöé 
Γöé Γöé export { composeRefs, useComposedRefs };
Γöé Γöé 
Γöé ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
Γöé
Γöö Run without --dry-run to apply.
